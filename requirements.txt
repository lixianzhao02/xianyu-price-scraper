# 闲鱼NS卡带价格爬虫 - Python依赖
# 安装: pip install -r requirements.txt
# 首次还需: python -m playwright install chromium（Playwright 模式需要）
# API 模式需要 Node.js 18+（用于执行 JS 签名算法）

# Playwright 模式（默认）
playwright>=1.40.0

# API 模式（--api 标志）
execjs>=1.5.1
blackboxprotobuf>=3.0.0
qrcode>=7.4.0

# 通用依赖
openpyxl>=3.1.0
requests>=2.28.0
PyYAML>=6.0
pandas>=2.0.0
pymysql>=1.0.0
numpy>=1.24.0
